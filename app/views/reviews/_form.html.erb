<%= form_for([@project, @project.reviews.build]) do |f| %>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content, rows: 4 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
