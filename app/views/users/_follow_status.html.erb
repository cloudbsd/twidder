<% @user ||= current_user %>
<div class="stats">
  <table>
    <tr>
      <td>
        <a href="<%= followees_user_path(@user) %>">
          <span id="followees" class="stat">
            <%= @user.followees.count %> following
          </span>
        </a>
      </td>
    </tr>
    <tr>
      <td>
        <a href="<%= followers_user_path(@user) %>">
          <span id="followers" class="stat">
            <%= pluralize(@user.followers.count, "follower") %>
          </span>
        </a>
      </td>
    </tr>
  </table>
</div>
